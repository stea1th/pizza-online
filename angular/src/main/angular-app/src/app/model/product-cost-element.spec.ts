import { ProductCostElement } from './product-cost-element';

describe('ProductCostElement', () => {
  it('should create an instance', () => {
    expect(new ProductCostElement()).toBeTruthy();
  });
});
